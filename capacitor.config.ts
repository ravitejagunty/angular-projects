import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ngcapacitor.app',
  appName: 'ng-capacitor-app',
  webDir: 'dist/ng-capacitor-app/browser'
};

export default config;
